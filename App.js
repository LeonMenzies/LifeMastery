import { RecoilRoot } from "recoil";

import { Navigator } from "~components/navigator/Navigator";

const App = () => {
  return (
    <RecoilRoot>
      <Navigator />
    </RecoilRoot>
  );
};

export default App;
